<h1>Listing items</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.id %></td>
    <td><%= item.value %></td>
    <td><%= link_to 'Show', item_path(id:item.id,group_key:item.group_key) %></td>
    <!-- <td><%#= link_to 'Show', item %></td>
    <td><%#= link_to 'Edit', edit_item_path(item) %></td>
    <td><%#= link_to 'Destroy', item, confirm: 'Are you sure?', method: :delete %></td>-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_item_path %>
